extends ../layouts/application.pug

block header
  = t('views.users.edit.user')

block content
  +formFor()(action=`/users/${user.id}/edit`, method='post')
    +input(user, 'firstName', errors)(type="text")
    +input(user, 'lastName', errors)(type="text")
    +input(user, 'email', errors)(type="text")
    +input(user, 'password', errors)(type="password")
    input.btn.btn-primary(type="submit", value=t("views.users.edit.submit"))