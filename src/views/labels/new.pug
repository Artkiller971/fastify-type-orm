extends ../layouts/application.pug

block header
  = t('views.labels.new.creation')

block content
  +formFor()(action='/labels' method='post')
    +input(label, 'name', errors)(type="text")
    +submit(label)(value=t('views.labels.new.submit'))